# 焙小满——你的 AI 烘焙助手

## 一句话介绍

焙小满（Satisfaction of Baking）：一款面向烘焙新手和爱好者的 AI 助手——用户在刷抖音烘焙视频时暂停画面，AI 自动识别甜品主体、检索同类教程、分析评论区真实经验，并根据用户个人条件（工具、食材、口味、能力）生成一份可执行的个性化图文配方，帮用户「一键做同款」。

> 把短视频时代的碎片经验，转化为属于你的定制成功路径。

---

## 一、真实场景：为什么用户需要这个产品

### 场景触发

用户在抖音刷到一条烘焙视频——巴斯克芝士蛋糕、泡芙、雪媚娘——画面诱人，评论区一片"已收藏等做"。用户当下产生强烈的复刻冲动：**"这个看起来好好吃，我也想做。"**

但从「想做」到「做成功」之间，存在一条巨大的鸿沟。

### 真实痛点

| 痛点 | 具体表现 | 频率 |
|------|----------|------|
| 信息碎片化 | 视频只展示关键画面，完整配方需要暂停抄写、拼凑多个来源 | 每次制作 |
| 参数冲突 | 不同博主给出不同温度、时间、材料比例，用户无法判断谁对 | 高频 |
| 评论区价值未利用 | 数千条评论藏着成功秘诀和失败教训，但用户没时间逐条看 | 每个视频 |
| 个人条件无法适配 | 用户可能只有空气炸锅、没有裱花袋、想少糖，教程不管这些 | 普遍 |
| 失败成本高 | 一次失败浪费 2-3 小时 + 几十元食材，且不知道为什么失败 | 新手尤甚 |

### 为什么触发是自然的

用户刷短视频时**天然处于「视觉激发」状态**——看到成品画面激发食欲和动手欲望。此时暂停画面是最自然的动作：

- 想看清材料比例 → 暂停
- 想记录步骤细节 → 暂停
- 想截图保存 → 暂停

**焙小满的入口就在这个自然暂停动作上**：当用户暂停视频时，弹出一个轻量提示——「想做同款？AI 帮你生成个性化配方」。不打断浏览节奏，但在用户最需要的时刻出现。

### 核心需求

我们解决的不是「帮用户找到配方」，而是：

> **帮用户判断：这个甜品，按照我的条件，到底能不能成功做出来——如果能，该怎么做。**

这是从「内容消费」到「实际完成」之间的最后一公里。

---

## 二、多模态必要性：为什么必须用视觉

### 视觉输入是唯一自然的触发方式

用户在刷视频时，**不知道自己看到的甜品叫什么名字**。

- 用户看到一个焦褐色表面的蛋糕，不一定知道它叫「巴斯克芝士蛋糕」
- 用户看到圆形空心的酥脆点心，不一定知道它叫「泡芙」还是「闪电泡芙」
- 用户看到白色糯米皮包裹水果的甜品，可能搜不到「雪媚娘」这个关键词

**如果只靠文字搜索，用户连搜什么都不知道。**

但暂停画面 → AI 看一眼就能识别。这正是多模态视觉能力的核心增益：

| 对比维度 | 纯文搜 | 视觉识别 |
|----------|--------|----------|
| 用户表达成本 | 需要知道甜品名称才能搜 | 暂停即可，零表达成本 |
| 理解准确度 | 搜索结果可能偏移 | 直接看画面，精准识别品类 |
| 触发自然度 | 需要跳出视频去搜索框输入 | 暂停就在当前场景内完成 |
| 信息获取 | 只能获取文字结果 | 同时获取画面上下文（食材、状态、工具） |

### 多模态在全链路中的作用

- **暂停帧识别**（视觉 → 文本）：AI 看画面识别甜品品类
- **步骤图生成**（文本 → 视觉）：AI 为每个操作步骤生成配图
- **语音输入**（语音 → 文本）：用户说出需求（"我想少糖，用空气炸锅"），比打字更自然
- **未来：制作状态判断**（视觉 → 文本）：用户拍照面糊状态，AI 判断"是否到位"

---

## 三、抖音生态放大：为什么这个产品适合在抖音做

### 抖音独有优势

| 生态资源 | 如何利用 |
|----------|----------|
| 海量烘焙教程视频 | 教程库天然存在，无需自建内容 |
| 丰富评论区数据 | 每个视频下数百至数千条真实评论，包含成功/失败经验 |
| 兴趣推荐算法 | 精准把烘焙教程推给有需求的用户，触发率高 |
| 短视频消费路径 | 用户「边看边想做」的消费心理天然匹配产品触发时机 |
| 达人内容生态 | 头部达人教程质量高、互动数据丰富，作为可靠信息源 |
| 抖音商城 | 识别到用户缺少的食材/工具后可直接跳转购买 |

### 站内闭环

```
兴趣推荐 → 用户刷到烘焙视频 → 暂停触发 AI 助手
    ↓
AI 分析视频 + 评论区 → 生成个性化配方
    ↓
用户制作 → AI 实时陪伴（拍照问状态）
    ↓
制作完成 → AI 复盘 → 鼓励发布成品视频
    ↓
成品视频回流抖音生态 → 更多用户看到 → 循环放大
```

这个闭环只有在抖音站内才能完成：视频内容供给、评论区数据、兴趣分发、商城转化、内容回流全在一个平台内。

### 入口设计

当用户在抖音内暂停一条烘焙类视频时，底部轻量弹出提示卡片：

> 🍰 「识别到你在看巴斯克芝士蛋糕，想做同款？让 AI 为你定制配方」

- 不遮挡视频画面
- 不打断浏览流程
- 只在烘焙类视频中出现（通过标签/内容理解判断）
- 用户可忽略，也可点击进入

---

## 四、AI 链路详解：6 个 Agent 如何协作

### 整体 Workflow

```
┌─────────────────────────────────────────────────────────────┐
│                    用户暂停视频                              │
│                        ↓                                     │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ ① Vision Agent（视觉识别）                           │   │
│  │   输入：暂停帧截图                                    │   │
│  │   模型：meta-llama/llama-4-scout（多模态）            │   │
│  │   输出：dish_name + category_key + confidence + reason│   │
│  └──────────────────────────────────────────────────────┘   │
│                        ↓                                     │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ ② Retrieval Agent（教程检索+AI重排）                 │   │
│  │   输入：品类标签 + 教程库全量候选                      │   │
│  │   模型：qwen/qwen2.5-vl-72b-instruct                 │   │
│  │   过程：对所有视频做语义相关度判断，输出 score+reason  │   │
│  │   输出：top-k 相关教程列表（带检索理由）              │   │
│  └──────────────────────────────────────────────────────┘   │
│                        ↓                                     │
│  ┌────────────────────────┐  ┌────────────────────────┐    │
│  │ ③ Extraction Agent     │  │ ④ Comment Agent        │    │
│  │  （教程信息抽取）       │  │  （评论洞察分析）      │    │
│  │  输入：视频标题/描述    │  │  输入：真实评论top100  │    │
│  │       + 热门评论        │  │       （按点赞排序）   │    │
│  │  输出：结构化配方       │  │  输出：成功经验        │    │
│  │   - 材料及用量          │  │   - 失败原因+解决      │    │
│  │   - 工具                │  │   - 工具/材料替代      │    │
│  │   - 步骤+状态判断       │  │   - 高频FAQ            │    │
│  │   - 温度时间            │  │   - 风险预警           │    │
│  └────────────────────────┘  └────────────────────────┘    │
│                   ↓                    ↓                     │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ ⑤ Recipe Agent（基础配方综合）                        │   │
│  │   输入：③的抽取结果 + ④的评论洞察                     │   │
│  │   模型：qwen/qwen2.5-vl-72b-instruct                 │   │
│  │   逻辑：综合多教程，取共识值，冲突用范围表达          │   │
│  │   输出：基础配方（材料/步骤/工具/避坑/FAQ）           │   │
│  │   约束：不编造来源中没有的数值                         │   │
│  └──────────────────────────────────────────────────────┘   │
│                        ↓                                     │
│              用户输入个性化需求                               │
│     （文本输入 或 语音输入 → ASR转文本）                     │
│                        ↓                                     │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ ⑥ Personalization Agent（个性化配方生成）             │   │
│  │   输入：基础配方 + 用户需求 + 评论洞察                │   │
│  │   模型：qwen/qwen2.5-vl-72b-instruct                 │   │
│  │   逻辑：重新计算材料比例、替换工具、调整时间          │   │
│  │         保留关键工艺步骤，不能删除核心环节            │   │
│  │         如果用户要求降低成功率，明确提示风险          │   │
│  │   输出：个性化图文配方 + AI生成步骤图                 │   │
│  │   校验：检查输出是否包含 ingredients/steps/pitfalls    │   │
│  │         不完整则自动重试一次                           │   │
│  └──────────────────────────────────────────────────────┘   │
│                        ↓                                     │
│            前端渲染完整图文配方页面                           │
│   （材料清单·工具推荐·步骤图文·避坑指南·FAQ）               │
└─────────────────────────────────────────────────────────────┘
```

### 各 Agent 详细说明

#### ① Vision Agent · 视觉识别

**为什么需要**：用户看到的是画面，不是文字。很多时候用户不知道甜品的准确名称。AI 通过视觉直接理解画面内容。

**技术实现**：
- 前端在 video 元素暂停时，通过 canvas 截取当前帧，转为 base64 JPEG
- 发送到后端 `/api/identify`
- 后端调用多模态视觉模型（meta-llama/llama-4-scout）
- Prompt 指定可选品类列表（来自 `category_aliases.json`），要求输出品类名+置信度+判断理由
- 识别成功后同时更新本地教程库的品类标签（自动积累标注）

**输出示例**：
```json
{
  "dish_name": "巴斯克",
  "category_key": "basque",
  "confidence": 0.92,
  "reason": "画面中有焦褐色表面的芝士蛋糕，切面绵密",
  "source": "vision_ai"
}
```

**兜底机制**：视觉识别失败时，用视频标题+描述做关键词匹配。

#### ② Retrieval Agent · 教程检索

**为什么需要**：不依赖单一视频信息。综合同品类多个教程才能得出可靠配方——不同博主的做法互相补充和验证。

**技术实现**：
- 读取教程库（`data/videos.json`）全量 10 个视频
- 调用 AI 对每个候选视频做语义相关度判断
- Prompt 要求 AI 根据标题和描述判断哪些视频是目标品类的教程
- 输出 score（0-1）和 reason，按 score 降序
- 只保留 score ≥ 0.5 的视频

**不是简单标签过滤**：即使视频标题没有直接写品类名，AI 也能从描述中判断相关性。比如标题是「#双重抹茶巴斯克」虽然关键词不完全匹配，AI 能识别这是巴斯克的变体。

#### ③ Extraction Agent · 信息抽取

**为什么需要**：视频描述和评论区包含大量有价值信息，但格式混乱。需要 AI 从非结构化文本中提取结构化配方。

**输入**：
- 每个召回视频的标题、描述
- 该视频评论区 top100 热门评论（按点赞数排序）

**提取目标**：
- 材料及用量（如"奶油奶酪 250g"）
- 工具列表
- 步骤流程（含关键状态判断点）
- 温度和时间
- 评论中用户提到的实操提示

**约束**：不编造来源中没有的精确数值。评论区用户提到的克重/温度是有价值信息，但要标注来源。

#### ④ Comment Agent · 评论洞察

**为什么需要**：评论区是「众包实验数据」——数千人尝试后的真实反馈。传统教程只记录成功路径，评论区同时包含失败路径。

**输入**：同品类所有视频的真实评论（按点赞排序取 top100/每视频）

**输出结构**：
```json
{
  "success_cases": ["蛋液分次加入更容易成功", "烤前喷水可以帮助膨胀"],
  "failure_reasons": [
    {"problem": "出炉塌陷", "reason": "烘烤不足或中途开门", "solution": "前段不开门，外壳烤干再取出"}
  ],
  "substitutions": [
    {"need": "没有裱花袋", "suggestion": "厚保鲜袋剪小口"}
  ],
  "faq": [
    {"question": "可以用空气炸锅吗", "answer": "可以，温度降20度，时间延长5分钟"}
  ],
  "risk_alerts": ["新手最容易在判断面糊状态时出错"]
}
```

**核心价值**：让用户在制作前就知道「别人踩过哪些坑」，而不是自己踩了才知道。

#### ⑤ Recipe Agent · 基础配方综合

**为什么需要**：单个视频的信息可能不完整或有偏差。综合 3-4 个同品类教程 + 评论区洞察，得到的配方更稳定、更可靠。

**综合逻辑**：
- 材料用量：多个来源接近的取共识值，差异大的用范围表达（如"55-60g"）
- 步骤：取最完整的步骤流程，补充其他教程的关键状态判断
- 避坑：从评论洞察中提取最高频的失败模式
- 工具：取并集

**输出**：一份完整的基础配方 JSON，包含材料、工具、步骤（含状态判断和风险）、避坑指南、FAQ。

#### ⑥ Personalization Agent · 个性化生成

**为什么需要**：每个用户的条件不同。基础配方回答「怎么做最稳」，个性化配方回答「按我的条件怎么做能成功」。

**个性化维度**：
- 工具适配：烤箱 → 空气炸锅（温度降低、时间调整、容量限制）
- 材料替代：没有裱花袋 → 厚保鲜袋；没有淡奶油 → 牛奶+黄油
- 口味调整：少糖/无糖/低脂（影响上色、口感、稳定性）
- 能力适配：新手 → 增加状态判断描述、简化操作、强化风险提醒

**关键约束**：
- 不能删除核心工艺步骤（如泡芙的烫面、分次加蛋液）
- 如果用户要求会显著降低成功率，必须明确提示风险
- 温度时间不确定时用范围+状态判断表达

**输出校验**：检查返回的 JSON 是否包含 ingredients/steps/pitfalls，不完整则自动重试。

### 图片生成补充

步骤图文中的配图通过 AI 生图模型（google/gemini-2.5-flash-image）生成：
- 为每个步骤生成对应的厨房场景图
- 预处理阶段批量生成并缓存，演示时直接加载
- 图片风格：真实厨房场景、俯视角度、暖色调、清晰展示操作状态

---

## 五、满足判断与质量保证

AI 链路不是跑完就结束。每个环节都有满足判断：

| 环节 | 满足条件 | 不满足时的处理 |
|------|----------|----------------|
| 视觉识别 | confidence ≥ 0.5 且命中已知品类 | 降级到标题/描述关键词匹配 |
| 教程检索 | 至少找到 2 个 score ≥ 0.5 的教程 | 放宽阈值或返回全量候选 |
| 信息抽取 | 返回的 ingredients 和 steps 非空 | 重试一次，仍失败则用基础教程数据 |
| 评论洞察 | 返回结构化洞察（非空） | 跳过洞察，用基础配方直接生成 |
| 基础配方 | ≥3 种材料 + ≥3 个步骤 | 重试，仍不满足则结合通用知识补充 |
| 个性化配方 | 包含 ingredients + steps + pitfalls | 重试一次，仍不满足则返回基础配方+简单调整 |

### 缓存与预处理策略

为保证演示流畅性和结果稳定性：
- 教程抽取、评论洞察、基础配方在首次运行时预处理并缓存
- 缓存存储在 `data/generated/` 目录
- 演示时只有「视觉识别」和「个性化生成」需要实时调用 AI
- 识别约 8 秒，个性化约 15-20 秒，其余秒出

---

## 六、增长策略与 ROI

### 冷启动方案

**阶段一：品类验证（1-2 周）**
- 聚焦 3 个高热品类：巴斯克芝士蛋糕、泡芙、雪媚娘
- 入口：在这 3 个品类的热门视频下展示「做同款」按钮
- 目标：验证用户点击率和配方完成率

**阶段二：扩品类+建心智（1-2 月）**
- 扩展至 10-20 个烘焙品类
- 建立用户心智：暂停烘焙视频 = 获得个人配方
- 通过达人合作验证效果（达人出教程+焙小满出个性化配方）

**阶段三：规模化（3-6 月）**
- 自动覆盖所有烘焙类视频
- 积累用户烘焙档案，提升个性化精准度
- 打通制作陪伴和成品复盘链路

### 核心指标

| 指标 | 定义 | 目标 |
|------|------|------|
| 触发率 | 暂停后点击「做同款」的比例 | ≥ 8% |
| 配方完成率 | 进入流程后走到最终配方页的比例 | ≥ 60% |
| 制作转化率 | 获得配方后实际去做的比例 | ≥ 25% |
| 成功率 | 按配方制作后成功的比例 | ≥ 75% |
| 次日留存 | 使用后次日再次打开的比例 | ≥ 30% |
| 配方保存率 | 用户保存配方的比例 | ≥ 50% |
| 分享率 | 成品发布回抖音的比例 | ≥ 10% |

### ROI 分析

**投入**：AI 调用成本约 $0.01-0.02/次完整链路（预处理后单次只需识别+个性化）

**产出**：
- 用户停留时长提升（从刷过 → 深度互动）
- 搜索渗透提升（用户主动搜索烘焙相关内容意愿增强）
- 商城转化（食材/工具推荐直接带货）
- 内容回流（成品视频发布增加平台内容供给）

---

## 七、完整闭环：从消费到创作

焙小满不只是一个配方生成工具，而是打通了从内容消费到内容创作的完整闭环：

### 阶段一：制作前（当前已实现）

```
刷视频 → 暂停识别 → AI 搜索召回 → 教程抽取 → 评论洞察 → 基础配方 → 个性化配方
```

用户获得：一份完全适合自己条件的图文配方。

### 阶段二：制作中（规划中）

```
用户制作过程中 → 拍照当前状态 → AI 实时判断
   "我的面糊这样正常吗？" → AI："面糊偏稀，建议再加少量面粉"
   "这个颜色可以出炉了吗？" → AI："颜色偏浅，建议再烤 3-5 分钟"
```

用户获得：实时的 AI 烘焙教练，降低失败率。

### 阶段三：制作后（规划中）

```
用户上传成品照片 → AI 复盘
   口味复盘："甜度适中，但口感偏干，下次可以多加 10ml 淡奶油"
   造型复盘："表面开裂原因是温度过高，下次降 10 度"
   鼓励分享："你的巴斯克做得很棒！发到抖音让更多人看到？"
```

用户获得：持续进步的烘焙档案 + 分享动力。

### 阶段四：生态回流

```
用户发布成品视频 → 回流抖音内容池 → 兴趣推荐给更多用户
   ↓
更多用户看到 → 触发焙小满 → 形成增长飞轮
   ↓
对有价值的教程视频 → 推送更多流量（正反馈给优质创作者）
```

---

## 八、商业化路径

### 短期：电商带货

- AI 识别到用户缺少的食材/工具 → 推荐抖音商城商品
- 示例："你选择了空气炸锅方案，推荐这款适合烘焙的空气炸锅"
- 示例："配方需要奶油奶酪 250g，点击购买"

### 中期：达人合作

- 达人出教程视频 → 焙小满出个性化配方
- 达人视频下方嵌入「AI 定制配方」入口
- 提升达人视频的互动率和完播率

### 长期：烘焙服务平台

- 个人烘焙档案 + AI 推荐下一个适合尝试的品类
- 烘焙社区（用户分享自己的成功配方和改良版本）
- 线下烘焙课程/材料包的导流

---

## 九、技术架构

| 层级 | 技术方案 |
|------|----------|
| 前端 | 原生 JS + CSS，抖音风格移动端 UI |
| 后端 | Python 标准库 HTTP Server，零第三方依赖 |
| AI 视觉识别 | OpenRouter → meta-llama/llama-4-scout |
| AI 文本生成 | OpenRouter → qwen/qwen2.5-vl-72b-instruct |
| AI 图片生成 | OpenRouter → google/gemini-2.5-flash-image |
| 语音输入 | Web Speech API（浏览器原生） |
| 数据存储 | JSON 文件（视频索引、评论、生成缓存） |
| 预处理 | 离线脚本批量运行教程抽取+评论分析+配方综合+步骤图生成 |

### 数据规模

- 10 个教程视频（3 巴斯克 + 3 泡芙 + 4 雪媚娘）
- 8000+ 条真实抖音评论
- 12 张 AI 生成步骤配图
- 3 份预处理基础配方缓存

---

## 十、创新点总结

1. **视频内容 → 可执行任务**：不是总结视频，而是生成可以直接照着做的配方手册
2. **评论区经验结构化**：首次将用户评论中的成功/失败经验转化为制作决策参考
3. **多教程综合 > 单视频摘要**：综合多个教程取共识，比依赖单一信源更可靠
4. **个性化是重新计算，不是换文字**：根据用户条件重新规划材料比例、工具适配、时间调整
5. **完整闭环设计**：从消费→制作→复盘→分享，打通内容生态全链路
6. **预处理+实时结合**：保证演示流畅性的同时保留真实 AI 能力调用
